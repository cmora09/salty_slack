<nav class="nav justify-content-center">
	<% if user_signed_in? %>
		<p class="nav-link">Signed in as <%= current_user.username %></p>
		<%= link_to "Home", root_path, class: 'nav-link text-info' %>
		<%= link_to 'Account Settings', edit_user_registration_path, class: 'nav-link text-info' %>
		<%= link_to 'Sign Out', destroy_user_session_path, :method => :delete, class: 'nav-link text-info' %>
	<% end %>
</nav>